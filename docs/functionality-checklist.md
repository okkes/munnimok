# munni — functionality checklist

The high-level manual test inventory (#194): the CORE things to walk to
know each feature works — not a changelog. Feature changes update the
affected line (or add one only for a genuinely new core flow); details,
fixes and internals never become items. The rule lives in the
assistant's memory alongside the guide/tour maintenance rules.

## Identity & onboarding
- [ ] Sign in (web + native return), demo mode, offline profile
- [ ] Fresh signup walks onboarding (name, avatar, country) and an interrupted one resumes
- [ ] Session survives restarts; an expired session recovers or says so honestly
- [ ] App lock: set up, unlock with PIN and passkey, disable; a refresh honors the configured auto-lock delay

## Spaces & sharing
- [ ] Create a space (period, currency, start date); edit its identity; switch spaces
- [ ] Invite-lock toggle gates all sharing; invite an existing friend with a role; invite a new person from the members screen (accept joins the space)
- [ ] Members: view, change role, remove; the removed member is told once and lands in another space; a read-only member keeps pulling quietly (no eviction popup, writes park)
- [ ] Leave a space; history start date moves with honest consequences

## Home
- [ ] Balance band modes and per-account picks (safe-to-spend converts through the display lens); blocks render, reorder, hide; a sparse desktop home centers one wider column; tab returns render instantly (remount cache, one-shot fade); the debts block has no see-all (the card is the door); a new-transaction row arrives with the New lens on
- [ ] Quick-add FAB reaches all six doors
- [ ] Review nudge, new transactions, upcoming costs and notifications reflect reality; see-all lands on the Upcoming page (recurring + loan dues together)

## Transactions
- [ ] List: search (title + amount, highlighted), quick filters, filter sheet; filters survive a detail detour; with linked shown separately, same-day pairs sit together as one visual unit
- [ ] Transfer pairs collapse to one row; per-account view keeps both legs
- [ ] Add/edit a manual transaction end to end (amount math, account, category, counterparty — required for movement categories, date guard)
- [ ] Detail: recategorize, rename, counterparty set/remove, recurring/event links, notes, receipt, customize sections, delete
- [ ] Split a transaction into parts; edit parts; un-split; split categories (€ and %) on rows and parts
- [ ] Reimbursements: link both directions (with clamping errors), parts included; unlink restores

## Review
- [ ] Walk the queue: split leads the card, then category, counterparty (movement confirms require one; the row hides after an explicit plain filing), counter-transaction, recurring + event (appear once a category stands), notes — confirm and skip; titles wrap in full; the category picker can filter BY counter account (pick suggests that account)
- [ ] Memory pre-fills return; bulk "apply to similar" applies what it promised and resets to select-all on every new card; the per-sibling counter queue holds the deck and counts down

## Categories
- [ ] Browse and search the picker (parent names match, ◆ filter); manage: create/edit/delete customs with impact warnings; locked families refuse subs
- [ ] Account-typed rows only offer categories that fit

## Recurring & debts
- [ ] Detection inbox → accept walks the occurrence review; linked charges take the category (auto-attached ones adopt it too but stay unreviewed); patterns are per-account, one suggestion per steady amount, cross-account echoes deduped, each card names its source account; the recurring form quick-creates its counterparty and shows account faces
- [ ] Ranges (period/next/year) tell the truth; the year chart plots paid-to-now and estimate-from-now with tappable dots (Show transactions disables when a month has nothing behind it); Upcoming rows say the days left
- [ ] A loan account shows debt, plan, payments and payoff; lender detection lands on Debts

## Accounts & banks
- [ ] Global overview: two segments — the global pool and collapsible per-space cards (closed by default); defaults fold, echoes jump to the real row, archived shares say who stopped sharing; an unattached account offers "Attach to this space"
- [ ] Connect a bank (choice, consent, callback, nightly fetch, reconnect); imports (bank chooser, preview, progress, result → explicit attach); an import beside a bank link stays its OWN account until the explicit merge (which runs the reconcile)
- [ ] Attach/detach per space with type (the attach door lands on the final step; shared spaces warn before attach); rename locally vs globally; type change re-reviews that space only
- [ ] Edit an account (a manual balance edit records an adjustment transaction); delete manual and bank-fed accounts cleanly; the CASH WALLET default takes hand entries and can be deleted (no heal-back; the explicit Default pick revives it); funding pots create without a balance question

## Plans (budgets, goals, allocation, events)
- [ ] Budget lifecycle: create with categories, thresholds warn, carry-over works
- [ ] Goals fund and progress; allocation envelopes fill per period
- [ ] Events: create, attach transactions (select-all screen), per-day costs and drill

## Portfolio, insights & receipts
- [ ] Holdings buy/sell and valuation; overview/trends/insights drill correctly; a drill's transaction opens INSIDE the overview (right pane at lg) and the chosen period survives the detour; uncategorized reads gray everywhere; funding-filed recurrings get no leak advice
- [ ] Receipt capture (camera/webcam/upload) links to transactions; shopping connections pull receipts

## Splits (bill splitting) & friends
- [ ] Split session with a friend end to end (invite, expenses, settle)
- [ ] Friends: add by ID, accept, profile sheet (copy ID, remove)

## Settings & platform
- [ ] Space settings rows all lead somewhere sane; global settings: profile (account deletion narrates its progress), devices, language (EN/NL/TR), appearance cycle, export, push, tips
- [ ] Scrolling a search list dismisses the keyboard (the closing relayout waits for the finger to lift — no mid-scroll jump); multi-field forms keep it while scrolling
- [ ] PWA installs and updates; native shells build, deep-link back and capture photos
- [ ] Platforms and environments as committed config (2026-09-17 clean slate, infra/platforms/README.md): every platform (lcl = this computer, nas = Synology, rpi reserved) runs one shared stack (GlitchTip with its own db, Vaultwarden, pgAdmin, control, valkey, OCR — lcl adds the family Caddy) plus any number of environments (web, admin, api, OWN Logto, OWN postgres) described by infra/platforms/<p>/platform.json + envs/<env>.json (slot → ports 8380+100·slot…, channel dev|latest, app channel, features android/ios/push/logos/telemetry/pgadmin + banking + sign-in providers, store ids default app.munni.<platform>.<env>); stacks munni-<platform>-<env|shared>, hosts munni-<env>-<platform>[-api|-admin|-logto|-logto-admin] and <service>-<platform>, deep-link scheme munni-<env>-<platform>, GitHub environments <platform>-<env|shared>; the workflows derive their matrices from the config (infra/ci/matrix.mjs, loading nas stacks leniently without the domain secret); infra/secrets.manifest.json names every secret with owner (generated | operator | module), scope (env | platform | stack | wizard) and feature — no repository-level secrets or variables except the first-boot latch
- [ ] The setup wizard requires its helper and is organised as Platforms → Features & accounts (tiles, brand marks, Manage popup, Check via the helper; Save stores the value in the wizard's own store infra/rendered/wizard/.secrets.json and, for nas, into every GitHub environment the manifest says needs it) → Shared services per platform → Environments (Add environment form; a workspace per environment with Overview, Registrations, Phones, Access and Clean up tabs) → Leftovers by hand (store records, the DSM deploy account, DNS for an own domain, the APNs key); every manual path of the old pages is gone (no "doing it by hand" folds, no pasted M2M credentials or tokens, no typed admin subjects)
- [ ] iOS archives need one enabled registered device (2026-09-19): automatic signing archives against a development profile and Apple issues one only for a team with an enabled device ("Your team has no devices" ended nas-dev's first build — three iPhones registered, all disabled since the wipe); "Apple Distribution" under automatic style conflicts even from the project file (proven, reverted), so the App Store Connect key check counts the team's iOS devices (enabled/disabled) and warns with the one-click fix, the wizard's iOS Build refuses to dispatch without an enabled one, a run asks Apple first and skips the archive with a notice + step summary instead of failing on every push (.github/scripts/asc-devices.js), the archive step still names the fix if a run hits it, the Phones tab's iOS steps and the phones guide carry it; a key that cannot cloud-sign is named at the archive step as well as at export
- [ ] Admin access, discoverable (2026-09-19): the admin portal and the control cockpit say on their sign-in screen that they take the operator's own app account and that admin is granted per account in the wizard; the denied note says where (the environment's Access tab) and both consoles offer Sign out so the role rides on a new token; the Access tab lists the three steps with the app's and the portal's addresses, loads its users by itself (quietly, per page load) and the environment's todo shows whether anyone holds admin (tab pill "admin ✓" / "nobody yet"); the vault folder carries an "Admin portal (no password)" secure note next to the Logto console item; the runbook names it
- [ ] Saving a sign-in tile re-renders the registration blocks (2026-09-19): Verify with Google/Apple enables the moment the tile is saved — no reload needed
- [ ] The platform vault files items reliably (2026-09-18): a folder is created first and every item is posted with its folderId (Vaultwarden's import endpoint only files through same-call folder relationships and left a folder's items unfiled on every run after the first); our own stray unfiled twins are removed on the way, the operator's own folders and items are never touched
- [ ] Registrations verify themselves (2026-09-18): the Registrations tab asks Google/Apple about every environment's URL on load, after a platform switch and after the provider tile is saved, keeps the verdict per platform in localStorage with its time (30 minutes fresh, shown as "checked 3 min ago"), and a provider without an API (Enable Banking) takes a done tick on the block that is the same item the Setup status rail lists; while the provider's sign-in tile is not saved the block says so and Verify is disabled (never a raw "missing: LOGTO_…" list); the Access tab of a NAS environment resolves its shared stack with the platform domain (it threw "PLATFORM_DOMAIN is not set" before)
- [ ] Apple, ordered (2026-09-18): the App Store Connect key tile asks for Admin access (cloud signing; the iOS run names the fix when a key cannot); the Apple sign-in tile is locked ("Not yet" + reason, chip Later, rail item unchecked) until the ASC key is saved, an environment with Apple sign-in exists and it has an https address, then its steps are computed from the config: the environment's App ID as primary, a per-platform Services ID name, every environment's domain and return URL, the key against the same App ID; a tile may declare ready() and compute its steps at render time
- [ ] iOS builds run on every push for the environments that enable iOS (2026-09-18): the legacy IOS_BUILD_ENABLED repository switch is gone, the config matrix decides like for Android; before exporting, the workflow asks App Store Connect whether the app record exists (.github/scripts/asc-app-record.js) and, when it does not, skips the upload with a notice and the step summary instead of an EXPORT FAILED — the signed archive still proves the pipeline
- [ ] Environments added after the shared stack heal themselves (2026-09-18): an environment's bootstrap pulls GLITCHTIP_API_TOKEN from the platform vault (the shared stack files it there) into its own GitHub environment and uses it in the same run, so its GlitchTip projects and DSN write-backs no longer wait for a re-run of the shared bootstrap; a shared bootstrap that finds a newer environment mirrors the value it already runs with instead of re-minting (a re-mint would strand the running GlitchTip); GLITCHTIP_SECRET_KEY, GLITCHTIP_ADMIN_PASSWORD and PGADMIN_PASSWORD are sharedOnly in the manifest and never mirrored; the phone Build sends no empty NATIVE_* variable (GitHub refuses them) and, on the NAS, refuses to build before NATIVE_LOGTO_APP_ID was written back
- [ ] Push has nothing to type (2026-09-18): the Firebase tile sits after the phone apps, is locked until the Android or iOS app is ticked and the Google Play account is saved, and its Check verifies the Play service account's two roles (switches the Firebase Management API on, reads the project; the missing role is named with the IAM link) — FCM_SERVICE_ACCOUNT_JSON is filled by Build, never asked for, never counted as missing; the secrets reveal wraps long values inside its card
- [ ] Platforms are independent (2026-09-18): the helper keeps one store per platform (values, GitHub token, upload keystore), the page keeps ticks and skips per platform, Save copies only into that platform's GitHub environments and a platform switch reconnects GitHub with that platform's token; the single exception is the Apple Development certificate, this computer's because Apple caps those per account, shipped into every platform's iOS environments; a feature tick only records the default for the next environment — nothing starts or gets created on its own, and LAN mode follows the ticks at the next Set up & start
- [ ] lcl platform: Set up & start mints and renders the shared stack, starts it, then every environment added in step 4 (none is created on its own) — Logto is seeded straight into the environment's database with the minted machine credentials and the bootstrap turns sign-in into code (apps, API resource with the `admin` scope, the `munni admin` role, connectors, branding, the console's admin); GlitchTip's admin + minted API token are created inside the container and the environment's five projects + DSNs follow; every minted credential lands in the platform's vault (one folder per stack); LAN mode moves the family onto https://munni-<env>-lcl.<ip-dashed>.sslip.io behind the family CA (trusted on this PC, carried by the phone apps); the helper keeps the family current (pull → re-render → images → up → self-restart) and can start at logon; Delete everything (wipe) destroys every stack with its volumes, the rendered dirs, the LAN marker and the environment files, then verifies the machine is clean; deleting one environment revokes its GoCardless consents, removes its GlitchTip projects and its containers, and forgets it
- [ ] nas platform, CI-first: the platform card stores the domain, the DSM deploy account (checked from here), the published path, the generated vault account and the optional registry token / ACME contact; Set up shared services creates nas-shared, copies the platform values into every environment, commits the config and dispatches Bootstrap (iac.yml) with a chained Deploy; the Bootstrap mints the shared secrets and mirrors the platform-scoped ones into every nas-<env>, writes the reverse-proxy rules through the DSM API, ensures the wildcard Let's Encrypt certificate (TLS-probed first; DSM's own wizard call, six-minute wait, lost answers polled out, never re-requested) and binds the rules, prepares the live dir (the PARENT of the published folder, resolved through the share's real path) and the poller Task Scheduler entry; Deploy (deploy-nas.yml — dispatched, and after every successful image build for the channel's environments) renders the bundle from the config, fills the env from the GitHub environment with every secret named explicitly (no toJSON(secrets)), uploads <stack>.tgz + VERSION_<STACK> through File Station, waits for the poller's marker and the seeded credentials, and re-runs the Bootstrap so app ids and DSNs are written back; adding an environment writes its JSON, creates nas-<env>, stores what its features need, commits and dispatches Bootstrap + Deploy; the NAS verdict is published as the repo variable NAS_STATE (flags + counts, never a host) and the helper probes every host from outside (DNS, certificate, rule, bundle) for the status rows; DSM refusals (402/105/119/103) carry operator advice and --verify prints the login shapes and session facts DSM accepts; pushes and image builds skip stacks whose GitHub environment does not exist yet (never bootstrapped) and a deploy job whose environment holds no domain yet ends with a notice, not a failure
- [ ] Cleanup as code per environment: lcl through the helper; nas through Bootstrap with cleanup=true (type-to-confirm in the wizard): GlitchTip projects, reverse-proxy rules (by uuid), the containers + volumes + folder through the poller (stamp "remove" → apply.sh down -v + rm -rf, marker "removed", waited for ≤10 min with the poller log printed), the GitHub environment and the environment's platform file (committed by the workflow); the shared stack goes last, once no environment is left (rules, containers, the poller task via SYNO.Core.TaskScheduler v2 delete, the live dir); the wildcard certificate stays; by hand: store records and, on a PC, the trusted roots of earlier https families (the Leftovers card names both, the Clean up log counts the roots via certutil)
- [ ] Feature tile header: mark, chip and toggle sit on one vertical centre
- [ ] A successful Save / Store closes the Manage popup by itself (0.7 s after the ✓, so the line is seen); the tile chip reads Saved ✓ and the verdict repeats under the health card; a failed or blocked store keeps the popup open with its message
- [ ] The step opens with the Integration health card: one line per integration the picked features need (GitHub included when it matters) with its state and a jump to its tile; Check all re-verifies every saved integration through the helper (stored values never reach the page; the registry token gained a helper-side check) and writes each verdict into its tile
- [ ] Optional integrations never block: the crash-mail SMTP url is optional (chip Optional, excluded from the secrets count, the rail's Blocking list and the family to-do); every tile has Skip for now — a skipped integration counts as done in the chip, the health card, the rail, the stepper and the feature tile until a value is saved (auto-unskip) or the skip is undone; a mail address pasted as SMTP url is named as such with the Gmail app-password recipe
- [ ] The header, the sticky stepper and the output drawer are constrained to the main column's width (1300px) — no more steps spread across an ultra-wide screen
- [ ] Phones per environment (any platform): one store identity per environment (app.munni.<platform>.<env>, changeable per platform side — Android and iOS may diverge — via the Phones tab), ONE Android flavor fed by the environment (MUNNI_APP_ID/SCHEME/LABEL/HOST; the google-services stub is rebranded per build and replaced by the real Firebase config when the wizard registered the apps), the iOS shell rebranded per build (bundle id, scheme, display name, the environment's web host as its associated domain); store readiness is polled, not clicked (Play app / ASC record / Firebase apps), the Apple App ID is registered with its capabilities, Firebase-enables the Play service account's own project and reuses it as the FCM sender, the wizard writes the environment's NATIVE_* variables into <platform>-<env> and dispatches native-android.yml / native-ios.yml with the stack (pushes build every environment on the branch's channel that enables the app); a missing store record is a warning, the signed artifact the deliverable; Retire withdraws the distribution (Play internal track cleared, TestFlight builds expired)
- [ ] Machine-owned Apple Development certificate: the first iOS Build mints it through mint-apple-cert.yml (environment input), the helper pulls the run artifact (p12 + serial) into the machine store, the wizard ships APPLE_DEV_CERT_P12/PASSWORD into env local (rewriting) — CI imports instead of minting; ONE certificate per Apple team: the mint never revokes the certificate its environment still holds, CI fails fast (naming the repair) when Apple no longer lists the imported one instead of minting throwaways, the prune runs as a sweep for leftovers, the wizard asks Apple by serial before each build and re-mints a revoked/expired one by itself, and skips minting when the repo already holds one at repository level
- [ ] Social sign-in setup as far as the providers allow (neither Google nor Apple has an API that creates an OAuth client / Services ID): the Google card deep-links the consent screen + client creation into the Play service account's project, both cards list every environment's callback (domain + URL for Apple) with copy buttons, Check/Save ask Google and Apple whether each callback is registered (a refused one warns and names itself), and the Apple Team ID falls back to the TestFlight card's; the Logto connectors live under their fixed ids (google-universal / apple-universal — a generated id made the documented callback a redirect_uri_mismatch; bootstrap replaces such an instance, sign-ins stay linked by target), and the App ID's Sign in with Apple capability carries the primary-app setting (without it Apple records nothing and keys/Services IDs find no identifier; the list, not a 409, decides what is enabled); an App ID pasted as the Apple client id is named as such (the web flow needs the Services ID), and Apple's own error message rides along with a refused return URL
- [ ] LOCAL builds show a "Trust this network's certificate" button + hint on the login screen (EN/NL/TR; derived from the sslip public origin, absent everywhere else); the hint walks BOTH platforms (Android CA install; iPhone: Profile Downloaded → Install, then Certificate Trust Settings → full trust); the ca site serves root.crt as an application/x-x509-ca-cert attachment so phones download it for the installer instead of rendering PEM text
- [ ] A sign-in that cannot start (e.g. the OIDC discovery fetch dies on an untrusted certificate) names the failure under the button (EN/NL/TR, reported to crash tracking) instead of a silently dead button; local builds append the certificate-trust hint
- [ ] A nonexistent repo name is created as a fork (different owner) or a template-generate copy (same account — forks cannot land in their own account), all branches included
- [ ] Operator consoles are two separate apps: the admin PORTAL (per environment: users, diagnosis, own-env bank consents with foreign-count note, quota) and the munni-CONTROL cockpit (shared level: every environment's consents grouped by origin, read-only, plus quota/health) — control never offers delete, the portal refuses deleting another environment's consent
- [ ] Admin access is the `admin` scope on the API access token (a Logto role on the API resource grants it — no config list, no grants table): /admin, /control and the catalog publish all gate on it; the consoles ask Logto for the scope and register their browser as a logged-in device (X-Munni-Device) like every client — a request naming no device is refused
- [ ] Admin access from the wizard: the Access tab lists the environment's Logto users (name, e-mail, id) with an Admin switch per user — the helper holds the environment's machine credential (lcl: its store; nas: read back from the platform's vault, folder <stack>) and assigns or removes the `munni admin` role through the Management API; no first user is created by the setup — sign in once, then switch yourself on
- [ ] Images are stack-agnostic (nothing baked, the runtime overlay configures every stack); the Machine-owned Apple Development certificate and the upload keystore live in the wizard's store and ship into every environment that builds the app (mint-apple-cert.yml takes the environment as its input)
- [ ] Clean slate (2026-09-17): no backwards-compatibility or migration code — one Dexie schema version, one EF Core Initial migration, no boot-time data folds/heals for old shapes (the live passes stay: catalog tombstones, bare-row category migration, default accounts, reimbursement normalisation), receipts v3 only, the transaction type is derived and never stored, every link carries its gate and type from the server, the bank provider is always named, every client names its device (X-Munni-Device); the legacy NAS stacks, the legacy GitHub environments/secrets/variables and the old local family were wiped; tests follow the sensible-tests rule (no "x was removed" tests, no feedback-round pins) with in-repo coverage thresholds (85 % lines) beside the Sonar gate
- [ ] Offline end to end: everything works, syncs on return, conflicts converge
